<% if @feed_items.any? %>
    <ol class="microposts">
        <%= render @feed_items %>
    </ol>

    <div class = "pag-bar mt-5 pt-5">
        <%== pagy_bootstrap_nav(@pagy_feed) if @pagy_feed.pages > 1 %>
    </div>
<% end %>